apiVersion: v1
kind: Service
metadata:
  name: frisco-frontend-service
spec:
  selector:
    app: frisco-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP